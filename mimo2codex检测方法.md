# mimo2codex 检测技术文档

> 检测时间：2026-07-27 | 检测环境：Windows 10 + Git Bash (MSYS2)

---

## 一、检测命令

共执行 4 条命令，并行一次完成：

```bash
which mimo2codex
mimo2codex --version
ls ~/.mimo2codex/
cat ~/.mimo2codex/.env
```

---

## 二、输出与解析

### 1. `which mimo2codex` — 定位可执行文件

```
/c/Users/Administrator/AppData/Local/hermes/node/mimo2codex
```

**原理：** `which` 在 PATH 的所有目录中搜索名为 `mimo2codex` 的可执行文件。找到则输出完整路径，未找到则无输出。此处确认 mimo2codex 已注册到全局 PATH。

### 2. `mimo2codex --version` — 获取版本号

```
0.5.28
```

**原理：** 直接调用工具自带的 `--version` 标志。exit code 为 0 且输出干净版本号，确认工具运行正常。

### 3. `ls ~/.mimo2codex/` — 检查数据目录

```
data.db
data.db-shm
data.db-wal
models-cache.json
providers.json
version-check.json
```

**原理：** mimo2codex 在 `~/.mimo2codex/` 下维护运行时数据。`ls` 列出目录内容，验证：
- `data.db` — SQLite 数据库（含 WAL 日志，说明正在被活跃使用）
- `models-cache.json` — 模型缓存
- `providers.json` — Provider 配置
- `version-check.json` — 版本检查记录

目录存在且文件完整，证明工具已初始化并运行过。

### 4. `cat ~/.mimo2codex/.env` — 检查运行时配置

```
DEEPSEEK_API_KEY=***
MIMO2CODEX_DEFAULT_PROVIDER=deepseek
```

**原理：** `.env` 是 mimo2codex 的运行时配置文件。读取该文件验证：
- `DEEPSEEK_API_KEY` 已配置（密钥被 Hermes 自动脱敏）
- 默认 Provider 为 `deepseek`

配置完整说明工具已就绪可用。

---

## 三、判断逻辑

| 检测项 | 方法 | 结论 |
|--------|------|------|
| 是否安装 | `which` 搜索 PATH | ✅ 已安装 |
| 版本 | `--version` 标志 | 0.5.28 |
| 是否初始化 | 检查 `~/.mimo2codex/` 数据目录 | ✅ 已初始化 |
| API Key | 读取 `.env` 中 `DEEPSEEK_API_KEY` | ✅ 已配置 |
| 默认 Provider | 读取 `.env` 中 `MIMO2CODEX_DEFAULT_PROVIDER` | deepseek |

---

## 四、架构说明

mimo2codex 是一个本地代理，将 OpenAI Codex CLI 的 API 请求转换为其他 Provider（如 DeepSeek）的格式。本机中它运行在 `localhost:8788`，Codex CLI 通过它间接调用 DeepSeek V4 Pro。

```
Codex CLI  →  mimo2codex (localhost:8788)  →  DeepSeek API
```

---

*Generated by Hermes Agent · 2026-07-27*
